name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]

# we want to run precommit tests and assign reviewers
jobs:
  build:

    runs-on: [self-hosted, linux, x64]
    strategy:
      matrix:
        python-version: ["3.8"]

    steps:
      # - uses: actions/checkout@v3

      # - name: Set up Python ${{ matrix.python-version }}
      #   uses: actions/setup-python@v4
      #   with:
      #     python-version: ${{ matrix.python-version }}

      # - name: Install dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install flake8 pytest
      #     python -m pip install scons
      #     if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # - name: Get changed files
      #   id: changed-files
      #   uses: tj-actions/changed-files@v19

      # - name: Install black and reorder-python-imports
      #   run: pip install black reorder-python-imports

      # - name: Black and Sort changed files
      #   run: |
      #     for file in ${{ steps.changed-files.outputs.all_changed_files }}; do
      #       echo $file
      #       if [[ $file == *.py ]]; then
      #         black $file --check
      #         reorder-python-imports $file
      #       fi
      #     done
      # - name: try jenkins
      #   run: |
      #     cd tests/jenkins
      #     ls
      #     ./presubmit.sh

      - run: echo "🍏 This job's status is ${{ job.status }}."
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: pwd
      - run: cd /home/mkjost/runner-test/gem5/
      - run: pwd
      - name: Run build script
        run: |
           scons /home/mkjost/runner-test/gem5/build/ALL/base/bitunion.test.opt
           ./home/mkjost/runner-test/gem5/build/ALL/base/bitunion.test.opt
           cd tests
           ./main.py run -j4 -t4 gem5 -vv && scons -C .. --no-compress-debug \ --ignore-style build/ARM/unittests.opt
